# Desktop app (Vite reads VITE_ prefixed vars)
VITE_VERCEL_API_URL=https://your-vercel-app.vercel.app

# Vercel serverless functions (set in Vercel dashboard, not in frontend .env)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net
MONGODB_DB=companies_db
